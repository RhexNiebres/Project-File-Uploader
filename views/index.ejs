<!DOCTYPE html>
<html lang="en">
  <body>
    <%- include ("partials/header")%>

    <h1>UploadEase</h1>
    <% if (user) { %>
    <h2>welcome back! <%= user.username %> 🎉</h2>
    <%- include("partials/upload")%> <%- include("partials/folders", { folders
    }) %>
    <h3>Uploaded Files:</h3>
<ul>
  <% if (files.length > 0) { %>
    <% files.forEach(file => { %>
      <li>
        <a href="/file/<%= file.id %>"><%= file.filename %></a>
        <a href="/file/<%= file.id %>/download" class="btn btn-primary">Download File</a>
        <form action="/file/<%= file.id %>/delete" method="POST" style="display:inline;">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </li>
    <% }) %>
  <% } else { %>
    <p>No files uploaded yet.</p>
  <% } %>
</ul> 
    
    

    <% } else { %>
    <a href="/login">get started </a>
    <% } %>
  </body>
</html>
